import { Button } from "@mantine/core";
import "./ShareButton.module.css";

export const ShareButton = () => {
  return (
    <Button className="pulse-button"  variant="filled" radius={"lg"}>
      Share
    </Button>
  );
};
